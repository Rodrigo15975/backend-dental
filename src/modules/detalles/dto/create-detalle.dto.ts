export class CreateDetallesDto {
  // trae el id del select
  estado_tratamiento: string;
  // trae el id del select
  medico: string;
  // trae el id del select
  servicio: string;
  // dato normal
  costo_servicio: string;
}
